const nameFieldChecked = true; // yes, name field is checked
const ageFieldChecked = false; // no, age field is not checked

const isGreater = 4 > 1;

console.info(isGreater); // true (the comparison result is "yes")