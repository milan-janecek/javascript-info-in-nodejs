'use strict';

// Math.random - Returns a random number from 0 to 1 (not including 1)

console.log(Math.random()); // 0.1234567894322
console.log(Math.random()); // 0.5435252343232
console.log(Math.random()); // ... (any random numbers)

// Math.max(a, b, c...) / Math.min(a, b, c...) - Returns the greatest/smallest from the arbitrary number of arguments

console.log(Math.max(3, 5, -10, 0, 1)); // 5
console.log(Math.min(1, 2)); // 1

// Math.pow(n, power) - Returns n raised the given power

console.log(Math.pow(2, 10)); // 2 in power 10 = 1024

// full Math docs - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math
